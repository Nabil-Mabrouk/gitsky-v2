# Fichier géré par Copier — ne pas éditer à la main.
# Il enregistre les réponses + la version du template (_commit, _src_path),
# ce qui rend `copier update` possible (propagation des évolutions du template).
{{ _copier_answers|to_nice_yaml }}
