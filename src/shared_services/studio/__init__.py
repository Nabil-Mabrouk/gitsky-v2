"""GitSky Studio — service partagé de direction artistique de flotte (Chap 24).

Produit le Creative Manifest (brief + blocs + médias + provenance) qui pilote la
vitrine d'un projet. Le manifest est gelé et versionné ; le générateur le
consomme de façon déterministe (il enrichit config.yaml, ne remplace pas la
génération).
"""
