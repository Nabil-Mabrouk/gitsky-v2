# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Env / secrets
.env
# ... sauf le .env par défaut du template frontend (non secret, versionné)
!src/generator/template/frontend/.env

# Node / frontend build
node_modules/
dist/
*.tsbuildinfo

# SQLite (dev + artefacts de test)
*.db

# Livre : HTML intermédiaire du build PDF (le .pdf, lui, est versionné)
docs/Template-book/pdf/gitsky-book.html

# OS
.DS_Store
Thumbs.db

# Outillage local (config MCP dev, non livrée)
.vincent/
